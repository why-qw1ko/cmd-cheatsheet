{
  "name": "git switch",
  "aliases": [],
  "domain": "git",
  "category": "config",
  "tags": ["switch", "branch", "checkout"],
  "description": "切换分支（Git 2.23+ 引入的现代命令，替代 git checkout 的分支操作）。",
  "syntax": "git switch <分支名>",
  "example": "git switch -c feature-x",
  "scenarios": [
    "切换到已有分支（git switch dev）",
    "创建并切换到新分支（git switch -c feature-x）",
    "切换回上一个分支（git switch -）",
    "基于远程分支创建本地分支（git switch -c dev origin/dev）"
  ],
  "related": ["git checkout", "git branch", "git merge"]
}
