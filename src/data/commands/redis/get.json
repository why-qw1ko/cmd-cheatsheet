{
  "name": "GET",
  "aliases": [],
  "domain": "redis",
  "category": "config",
  "tags": ["string", "key-value", "read"],
  "description": "获取指定键的值。如果键不存在返回 nil。如果值不是字符串类型则报错。",
  "syntax": "GET key",
  "example": "GET session:abc123",
  "scenarios": [
    "读取缓存数据",
    "获取会话信息",
    "检查键是否存在并读取值",
    "配合 SET 实现简单的键值存储"
  ],
  "related": ["SET", "MGET", "GETRANGE", "STRLEN"]
}
