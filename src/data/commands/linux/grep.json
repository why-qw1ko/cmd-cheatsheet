{
  "name": "grep",
  "aliases": ["egrep", "fgrep"],
  "domain": "linux",
  "category": "text",
  "tags": ["search", "regex", "filter", "pattern"],
  "description": "在文件或标准输入中搜索匹配指定模式的行。支持基础正则（BRE）、扩展正则（ERE）和固定字符串匹配，是文本处理三剑客之一。",
  "syntax": "grep [选项] 模式 [文件...]",
  "scenarios": [
    "在日志文件中搜索错误信息（grep ERROR app.log）",
    "递归搜索项目中包含某函数名的所有文件（grep -rn 'funcName' .）",
    "统计匹配行数（grep -c 'pattern' file）",
    "反向匹配，排除某些行（grep -v 'debug' file）"
  ],
  "related": ["awk", "sed", "find"],
  "example": "grep -rn --include='*.py' 'def main' ./src",
  "since": "Unix V4"
}
